// Operador ternario con if aninado

const autenticado = true;
const puedePagar = true;

console.log( autenticado ? puedePagar ? "Si puede pagar" : "Si autenticado pero no puede pagar" : "No puede pagar");